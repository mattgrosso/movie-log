---
name: User Preferences
description: Matt's working style and preferences observed across sessions
type: user
---

- Deploys via `yarn deploy` which builds locally and syncs to S3 + CloudFront invalidation. No CI/CD pipeline.
- Prefers AWS for infrastructure since the app already lives there (S3 + CloudFront).
- Comfortable working through AWS console step by step with guidance.
- Likes to see prompts and tweak them himself rather than have Claude iterate on them.
- Prefers unobtrusive UI — small buttons, nothing flashy.
- Quick to spot contrast/readability issues (caught black text on black background immediately).
